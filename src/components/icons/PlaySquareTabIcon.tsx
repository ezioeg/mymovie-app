import React from 'react';
import {PlaySquare} from 'lucide-react-native';

const PlaySquareTabIcon = ({color, size}: {color: string; size: number}) => {
  return <PlaySquare color={color} size={size} />;
};

export default PlaySquareTabIcon;
